{
	"type": "module",
	"devDependencies": {
		"@commitlint/cli": "^19.8.0",
		"@commitlint/config-conventional": "^19.8.0",
		"husky": "^9.1.7"
	},
	"scripts": {
		"prepare": "husky",
		"test": "echo \"Error: no test specified\" && exit 1"
	}
}
